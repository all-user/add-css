{
  "framework": "mocha",
  "src_files": [
    "*.coffee"
  ],
  "serve_files": [
    "./test/build.js"
  ],
  "before_tests": "npm run build && $(npm bin)/coffee -o ./test ./test/index.coffee && $(npm bin)/browserify ./test/index.js -o ./test/build.js",
  "after_tests": "rm ./test/*.js"
}
